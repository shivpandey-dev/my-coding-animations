import {interpolate,useCurrentFrame} from "remotion";import {ConnectionLine} from "../components/ConnectionLine";import {DatabaseNode} from "../components/DatabaseNode";import {SceneLayout} from "../components/SceneLayout";import {ServerNode} from "../components/ServerNode";import {ServiceHeader} from "../components/ServiceHeader";import {TrafficPacket} from "../components/TrafficPacket";import {clamp} from "../theme";
export const RDSScene=()=>{const f=useCurrentFrame();const p=interpolate(f,[25,75],[0,1],clamp);return <SceneLayout><ServiceHeader number="05" title="RDS" descriptor="MANAGED DATABASE"/><ServerNode x={130} y={990} delay={5}/><DatabaseNode x={720} y={990} delay={28}/><ConnectionLine startX={340} startY={1065} endX={720} endY={1065} progress={p} label="SELECT *"/>{[0,1,2].map(i=><TrafficPacket key={i} from={{x:340,y:1065}} to={{x:720,y:1065}} progress={(p+i*.3)%1}/>)}</SceneLayout>};
