import {interpolate,useCurrentFrame} from "remotion";import {ArchitectureNode} from "../components/ArchitectureNode";import {ConnectionLine} from "../components/ConnectionLine";import {Panel,SceneLayout} from "../components/SceneLayout";import {ServiceHeader} from "../components/ServiceHeader";import {TrafficPacket} from "../components/TrafficPacket";import {clamp,colors,mono} from "../theme";
export const ECRScene=()=>{const f=useCurrentFrame();const p=interpolate(f,[28,80],[0,1],clamp);return <SceneLayout><ServiceHeader number="10" title="ECR" descriptor="DOCKER IMAGE REGISTRY"/><Panel style={{position:"absolute",left:100,top:700,width:360,height:170,padding:30,boxSizing:"border-box",fontFamily:mono,fontSize:27,color:colors.green}}><span style={{color:colors.muted}}>$ </span>docker build<br/><span style={{fontSize:20,color:colors.muted}}>✓ app:v1.0</span></Panel><ArchitectureNode x={425} y={1030} label="app:v1.0" subLabel="CONTAINER IMAGE" delay={20} accent={colors.blue}><div style={{fontSize:48}}>⬡</div></ArchitectureNode><ArchitectureNode x={750} y={1030} label="ECR" subLabel="REGISTRY" delay={55}/><ConnectionLine startX={655} startY={1105} endX={750} endY={1105} progress={p}/><TrafficPacket from={{x:655,y:1105}} to={{x:750,y:1105}} progress={p} color={colors.blue} size={22}/></SceneLayout>};
